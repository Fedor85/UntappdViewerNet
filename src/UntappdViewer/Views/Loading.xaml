<UserControl x:Class="UntappdViewer.Views.Loading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:properties="clr-namespace:UntappdViewer.Properties"
             xmlns:mvvm="http://prismlibrary.com/"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ItemsControl Background="#000000" Opacity="0.5"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Label Content="{x:Static properties:Resources.Loading}" FontSize="25" Foreground="#ffffff"/>
            <Label x:Name="StatusBar" Content=".............."  FontSize="25" FontWeight="Bold" Foreground="#ffffff"/>
            <Label x:Name="StatusTimeBar" Content="time" HorizontalAlignment="Center" FontSize="15" Foreground="#ffffff"/>
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Grid.Row="1" Content="{x:Static properties:Resources.Cancel}" Command="{Binding CancelButtonCommand}"
                    Visibility="{Binding VisibilityCancelButton, Converter={StaticResource BooleanConverter}}" Width="50" Height="20" IsDefault="True"/>
        </Grid>
        <!--<Rectangle>
            <Rectangle.Fill>
                <SolidColorBrush Color="#fd9532" Opacity="0.5"/>
            </Rectangle.Fill>
        </Rectangle>
        <fa:ImageAwesome Icon="CircleOutlineNotch" Spin="True" Height="48" Width="48">
            <fa:ImageAwesome.Foreground>#ffffff</fa:ImageAwesome.Foreground>
        </fa:ImageAwesome>-->
    </Grid>
</UserControl>
